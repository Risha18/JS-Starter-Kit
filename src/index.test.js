// @ts-ignore
import { expect } from "chai";

var jsdom = require("node-jsdom");

import fs from "fs";

describe("my first test", () => {
  it("should pass", () => {
    expect(true).to.equal(true);
  });
});
